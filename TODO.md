# TODO：待辦事項

> 專案背景、設計原則、技術架構與部署流程分別見 `README.md`、`AGENTS.md`、`WORKFLOW.md`，本文件只放**未完成的待辦**。
> 最後更新：2026/8/29

> 優先級排序：§1、§2 為高（已定案/進行中），§4 為中（構想待討論），§3、§5 為低（可延後）。

---

## 1. RSS 災情抓取接入 build

`build/rss_sources.json` 已建好並實測（來源清單與抓取守則見 `build/rss_sources.json` 與 `AGENTS.md`「新聞 RSS 來源」），但 `build/site.py` 尚未有任何 RSS 邏輯。待實作：

- 批次抓取 `rss_sources.json` 的 `sources`（勿呼叫 `failed_sources`）。
- 解析器需同時相容 `rss20`（`<item>`）與 `atom`（`<entry>`，公視是 Atom）。
- 單一來源 404/超時不中斷 build（跳過＋記 warning）。
- 依時間過濾，只保留近期且與當前活動事件相關條目；每筆只給摘要＋原新聞連結。
- **風傳媒（storm.mg）待復查**：RSS 疑似移除，但它是颱風/災情最重要的新媒體之一。若確認失效，取不到時退而用 Obscura 抓其新聞頁。

## 2. 地圖紅警功能（構想，待實作；2026/8/28 討論定案）

在互動地圖上以紅色危險告警標註「目前或預計會有淹水/大雨的地區」，讓使用者一眼看到「現在最危險的地區在哪」。**分兩層、可獨立上線**：

### 2a. CWA 紅警層（**全自動部署**，已定案）

- **掛在現有 build 流水線上**：build 時本機抓 CWA → 合成 `map.geo.json`（polygons + points，每筆帶 `level: 🔴/🟡/🟢`、`type`、`source`、`time`）→ 產地圖頁 → 部署。**沿用現有 GitHub Actions 每 2 小時排程**，不需新排程、新 agent、新金鑰；顏色/等級閾值邏輯全部寫死在 build 端（`cwa.py`/新模組），前端只負責渲染。
- 資料來源對應：
  | 地圖元素 | 來源 | 備註 |
  |----------|------|------|
  | 雨量站紅點 | O-A0002-001 `GeoInfo` + `Past1hr`/當日累計 | 超閾值上紅，點大小/顏色深淺對應雨量等級 |
  | 颱風軌跡/風圈 | W-C0034-005 | 預測路徑＋風圈圓環 |
  | 海區警報多邊形 | W-C0034-001 CAP `area` | **唯一官方座標化多邊形，且是海區** |
  | 陸地紅區（豪大雨特報影響區域） | W-C0033-002/003 | ⚠️ 影響區域是**文字/縣市清單，非座標多邊形**，必須經 gazetteer 轉換；標籤註明生效時段（「今夜起」等） |
- **gazetteer（鄉鎮級靜態座標表）是必建項目，非可選**：JSON 存 repo（手工維護或用 CWA C-B0074 測站資料生成），一次性建好後此層即永久自動。特報文字與災情新聞的鄉鎮名稱都對照它；查不到回退縣級，再查不到不上圖。
- **不做**：像素級降雨預報雲圖（CWA 開放資料無 48h 雨量預報座標，F-C0033-001 已下架）；河川水位/土石流（O-C0010-001 已下架）——淹水類警報只能靠 2b 新聞層，粒度到鄉鎮。

### 2b. 災情新聞點層（人工/agent 餵料，可選疊層）

- 來源：RSS（見 §1）＋災情 markdown；鄉鎮名經 gazetteer 對照上圖，每筆附新聞來源連結。
- 維持人工把關（核心原則：災情新聞不自動推）；缺了不影響 2a CWA 層運作。

### 選型（已定：Leaflet + OpenStreetMap，非 Google Maps）

- 不用 Google Maps：JS API 計費需綁 billing account（意外帳單風險）、**必須連 Google 伺服器無法離線自駕**、API key 暴露前端。若日後嫌 OSM 瓦片太素，改換預渲染瓦片供應商（CARTO/OpenFreeMap），架構不變。
- Leaflet + 自託瓦片：開源免費、無 API key、靜態部署無縫。
- **保持輕量**：Leaflet 只在「地圖頁」載入（自託 `leaflet.js` ~40 KB + 1 個 CSS）；**首頁維持零 JS**（現有靜態 SVG 軌跡圖不換）。

### 離線自駕（硬需求）

- 所有 JS/CSS 自託在 `public/assets/`，不引 CDN。
- 瓦片策略：build 時只抓台灣範圍所需瓦片存本地（約 z9–z11，幾十張 PNG、數 MB）——瓦片幾乎不變，可 build 時缺什麼補什麼，甚至直接 commit 進 repo 使 build 零外部依賴；Leaflet 指向本地瓦片 → 完全離線、零外部請求（災發時網路最不稳定，離線靠得住）；不要跑 OSM tile server。出範圍的瓦片顯示空白底（或 fallback 到現有靜態 SVG）。

---

## 4. 平常時期區域性天氣報導（構想，待討論定案；2026/8/29 提出）

### 問題意識

目前專案是**純事件驅動**：有颱風或災情時，`颱風/` 和 `災情/` 的 markdown 檔案提供豐富的紀錄；但無事件時期，首頁僅剩下 `cwa.py` 產出的「氣象總覽」區塊（颱風軌跡、警報狀態、雨量），內容相當空蕩——只有 CWA 原始 API 資料的機械式呈現，缺少「彙整性」和「在地化」的視角。

台灣平均每年約 2~3 個颱風影響，颱風季之外仍有大量「無事件」的日數。使用者在這些時期造訪網站時，需要有意義的內容。

### 構想方向

建立平常時期的區域性天氣報導，作為事件發生時的「背景資料」與「基線」參考。

#### 短期：強化現有 CWA 區塊（方案 B，最小改动）

不改目錄結構，在 `cwa.py` 中增加「平常天氣摘要」的渲染邏輯，掛在首頁「氣象總覽」區塊下方：

- **今日雨量排行榜**：從 O-A0002-001 抓取自動氣象站資料，產出各縣市 Top 5 雨量
- **明日天氣重點**：從 F-D0047 鄉鎮預報彙整各區域預報重點（PoP、溫度 extremes）
- **特報狀態**：從 W-C0033 彙整目前生效的大雨/豪雨/強風特報清單
- 這些資料完全自動化（build 時抓 CWA → 寫入靜態 HTML），零新檔案

**優點**：零新檔案、零新目錄、完全自動化、低成本。
**缺點**：缺乏「解讀」層級，仍是純資料展示。

#### 中期：輕量「天氣日報」檔案（方案 A，推薦）

建立新目錄 `天氣/`，格式類似災情檔案但更簡化：

```
天氣/{YYYY}/{MM}/{MMDD}_{摘要}.md
```

內容結構（每次 2~3 頁 markdown）：
- **氣象重點摘要**（3~5 句，agent 根據 CWA API 自動生成）
- **各縣市天氣速報**（雨量 Top 5、溫度 extremes、特報狀態）
- **未來 3 天預報重點**（F-D0047 鄉鎮預報的彙整）
- **備註**（無異常時標註「本日無重大天氣事件」）

**關鍵設計**：
- 由 build 時自動產生（類似 `cwa.py` 的自動化邏輯），**不要求人工把關**
- 內容量控制在極小（2~3 頁），不增加 build 負擔
- 每 24 小時產出一筆（而非每 2 小時），降低維護成本
- 有事件時，該檔案自動標註「⚠️ 已轉入事件模式」，並連結到對應的颱風/災情檔案
- 無事件時期，這份檔案就是首頁「天氣重點」區塊的資料來源

**優點**：提供「解讀」層級、可作為事件發生時的基線資料（例如「本次降雨量是近 30 天平均的 5 倍」）。
**缺點**：需要新目錄結構與命名規範，需同步更新 AGENTS.md 與 build 系統。

### 建議執行順序

1. **先做方案 B**（強化 `cwa.py` 的平常天氣展示）——零新檔案，快速驗證效果
2. **再做方案 A**（`天氣/` 目錄的輕量日報）——作為方案 B 的「彙整版」
3. **事件期間的銜接**：`天氣/` 檔案自動標註狀態，連結到對應的颱風/災情檔案；事件結束後恢復日常模式

### 與現有架構的兼容性

- 不影響現有 `颱風/`、`災情/` 的事件驅動流程
- CWA API 抓取已自動化，不需新增金鑰或排程
- 可與 §2 地圖紅警功能共用 gazetteer（若有）
- 與 RSS 災情抓取（§1）獨立，不互相依賴

---

## 3. 移除 ja 產出，保留 i18n 架構（🟢 低優先級，2026/8/28 討論定案）

ja 頁目前只有 UI 詞彙是日文、內容（新聞/CWA 資料）全為中文，半殘體驗不如繁中頁＋瀏覽器整頁翻譯；且目標受眾基本不存在、維護成本雙倍。做法：

- **build 不再生成 `public/ja/`**、移除頁首語言切換（`build/site.py` 語言迴圈改回單語言）。
- **`build/i18n.py` 的 `STRINGS` 表與 ja 字串保留不動**——日後加任何語言（含復原 ja）只是開一行配置。
- **文件同步**：`AGENTS.md`「多語言」章節、`WORKFLOW.md` §3 驗證清單（刪「④ 雙語言輸出確認」）、§1 例行流程涉及 `/ja/` 的部分。
- 機翻自撰彙整文字（Google/DeepL，build 時）已評估但**作廢**（2026/8/28）：CWA 資料不該翻譯（條款＋正確性）、新聞摘要無翻譯授權（版權）；不列入待辦。


---

## 5. 事件分享按鈕（構想，待實作；🟢 最低優先級，2026/8/29 提出）

### 目的
讓使用者能從「事件頁」一鍵把該颱風/災情紀錄分享出去（Line、社群、複製連結等），提升災情傳播範圍。

### 分享內容
每筆事件分享出去的三樣資料（皆取自建站既有資料，不需新 API）：
- **正規 URL**：沿用 `build/site.py` 的 `link(e)`（`{SITE_BASE}/{'/'.join(e['url'])}`），例如 `https://weather.avpclub.eu.org/events/颱風/2026/08/0807_13_白海豚_DOLPHIN.html`。
- **標題**：`ev['name']`（例「颱風白海豚（DOLPHIN）— 2026 年第 13 號」）。
- **摘要**：`[狀態]・[severity]・[縣市]`（例「進行中・🔴重大・台南／高雄」），控制長度以便 Line 分享。

### 分享機制（三層降級）
| 層級 | 機制 | 適合情境 | 備註 |
|------|------|----------|------|
| 一 | **Web Share API** `navigator.share()` | 手機原生分享列 | 最優雅；需 https（本站符合）＋使用者觸發（按鈕點擊正合適）；不可用時退到二 |
| 二 | **複製連結到剪貼簿** | 桌面／API 不可用 | `navigator.clipboard` 失敗時退回到 `execCommand`，並顯示「已複製」提示 |
| 三 | **平台一鍵連結**（純 URL builder） | 想直達特定 App | 台灣首需 **Line**（`https://share-line.me/dialog/?text=&url`），其次 X/Twitter、Facebook；純 URL 拼裝、**無需 API key、無 CORS**，與 build 時抓取架構不打架 |

### OG / Twitter Card meta（體驗加分項，工作量最大）
目前事件頁沒有 OG 標籤，連結貼到 Line/Slack 僅顯示裸 URL。補上 `og:title / og:description / og:image / twitter:card` 後，分享預覽會變成有標題＋severity 徽章的卡片。需一張分享圖卡（可用事件 severity 色系生成簡單圖），可與此項一起或之後單獨立。

### 實作位置與方式
- 在 `build_event_page()` 的 hero section（放 `badge`＋`status`＋`chips` 的 `<section class="card hero">`）右側加分享按鈕。
- 加 i18n 字串：`share`、`copy_link`、`copied`（走現成 `t(lang, key)` 三級回退，ja 頁面也會有）。
- 內聯 JS + 現有 CSS 變數（`--accent` 等），自動適配深色／淺色主題。
- 純 build 期 Python 改動，不碰 CWA/API，符合「災情新聞人工把關」原則。

### 注意事項
- Web Share / clipboard 在非安全環境（http）不可用；本站已 https，無虞。
- Line 分享文字有長度上限，摘要需控制長度。
- 分享按鈕只放「事件頁」；首頁「事件 Hero 入口卡」是中性入口（無 severity 色系），**不放**（符合 AGENTS.md 設計意圖）。
- 建議執行順序：**A（Web Share ＋ 複製連結）→ B（＋ Line 一鍵）→ C（＋ OG 分享圖卡）**，目前全列為最低優先、暫未實作。
